# Events Feature

Place event definitions, runtime state, action execution, cutscene direction,
and vehicle behaviors here. Events should emit domain events or submit world
actions rather than directly owning UI.
