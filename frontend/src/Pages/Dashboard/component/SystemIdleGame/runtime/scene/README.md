# Scene Runtime Adapters

Use this folder for Phaser scene adapters such as preload, camera, input,
lighting, multiplayer bridge, and render sync as those files are split out of
the legacy runtime files.
